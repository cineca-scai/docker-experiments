
#################################
# Data containers definition

sqldata:
    image: busybox
    volumes:
        - /var/lib/postgresql/data
irodsdata:
    image: busybox
    volumes:
        - /var/lib/irods

# Note: i have not yet defined the right volumes.
# Work in progress