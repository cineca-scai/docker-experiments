
#################################
# Data containers definition

sqldata:
    image: busybox
    volumes:
        - /data
irodsdata:
    image: busybox
    volumes:
        - /test

# Note: i have not yet defined the right volumes.
# Work in progress